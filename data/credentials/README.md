# Credentials

Place Google OAuth client secrets (`google_calendar_credentials.json`) and the
generated refresh tokens (`token.json`) in this folder. Both files stay out of
version control; see `docs/GOOGLE_CALENDAR_SETUP.md` for details.

