# Storage Artifacts

`conversations.json` is written here by `ConversationStorage`. The file is
created automatically the first time the app runs. Do not commit real user
data; the filename is ignored via `.gitignore`.

